# Open Canopus server. The agent loop + MockProvider need nothing; local_server
# (the SBC / on-device tier) needs fastapi + uvicorn to serve the client + WS,
# and httpx for LlamaServerProvider to reach the local llama.cpp llama-server.
# The hosted CLOUD backend (Azure provider, Stripe billing) lives in a separate
# private repo and brings its own deps — none of it is required here.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
