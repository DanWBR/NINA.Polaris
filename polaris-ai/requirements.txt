# Training stack. Install a CUDA-matched torch separately — match your GPU:
#   RTX 50-series (Blackwell, sm_120): CUDA 12.8 wheels
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#     (if unavailable on stable, use the nightly cu128 channel)
#   Older GPUs: cu124/cu121 as appropriate. CPU-only `pip install torch` = no GPU.
torch>=2.2
torchvision>=0.17
numpy>=1.24
scipy>=1.10
tqdm>=4.65
pillow>=10.0
matplotlib>=3.7

# FITS + survey data
astropy>=6.0
astroquery>=0.4.7

# ONNX export + fp16
onnx>=1.16
onnxruntime>=1.18
onnxconverter-common>=1.14
onnxscript>=0.1            # required by torch.onnx.export in torch >=2.5
