{
  "_comment": "Optional external assistant module. The host stays a generic, off-by-default iframe host: it only fetches this manifest URL and renders whatever branding/allowlist the manifest supplies — no assistant-specific product logic lives in this open-source app. Empty disables it. A localStorage key 'polaris.assistant.manifestUrl' overrides this for development.",
  "manifestUrl": "https://polaris-assistant.bravecoast-379c8dce.brazilsouth.azurecontainerapps.io/api/manifest"
}
